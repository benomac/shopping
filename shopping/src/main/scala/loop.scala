
// // Scala program to illustrate how to 
// // create for loop using to  

// object Main extends App
// { 
//     case class Price (price: Int, numForDeal: Int, priceForDeal: Int)
//     val prices = Map('a' -> Price(50, 3, 130), 
//                     'b' -> Price(30, 2, 45), 
//                     'c' -> Price(20, 0, 0), 
//                     'd' -> Price(15, 0, 0))

    
 
    // def removeInvalidItems (items: String, products: Map[Char, Price]): String = {
    //     val valid = for (item <- items) yield {
    //         if(!prices.keySet.contains(item)) {
    //             println("invalid item")
    //             ""
    //         } else {
    //             item
    //         }
    //     }
    //     valid.mkString("")
    // }


//     println(removeInvalidItems("ahhjydhjdfdvadcccdcdseaa", prices))
// } 


// // def displayMessageForInvalidItems (items: String, products: Map[Char, Price]): String = {
// //         val valid = for (item <- items) yield {
// //             if(!prices.keySet.contains(item)) {
// //                 println(item + " is an ivalid item")
// //             } else {
// //                 item.toString
// //             }
// //         }
// //         valid.toString 
// //     }